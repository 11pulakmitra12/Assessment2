# Assessment2
Assessment for Incubyte

to start the server ---> run the command "npm run start"
to test all the test cases --->  run the command  "npm run test"
to start to test the add program ---> run the command "npm run test-add"
to start to test the other programs --> run the command "npm run test-programs"
to start to test the Api ---> run the command "npm run test-api"


